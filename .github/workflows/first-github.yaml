name: My GitHub workflow
on:
  push:
    branches:
      - main

env:
  PROJECT_NAME: githubactions-practice
  COURSE: LLMOps

jobs:
  myjob:
  runs-on: ubuntu-latest
  steps:
    - run: echo "checkout the repo"
    - name: Checkout Repo
      uses: actions/checkout@v4
    - run: printenv
    - run: echo "this is the env ${{env.PROJECT_NAME}} and ${{env.COURSE}} for demo."